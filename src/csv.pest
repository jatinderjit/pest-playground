//! A parser for CSV with numbers

/// Matches the integer part. It doesn't allow multiple preceding zeros.
integer = { !("0"{2,}) ~ ASCII_DIGIT+ }
fractional = { "." ~ ASCII_DIGIT+ }
non_neg = { (integer ~ fractional) | integer | fractional }
number = { "-"? ~ non_neg }
record = { number ~ ("," ~ number)* }
file = { SOI ~ (record ~ (("\r\n" | "\n") ~ record)*)? ~ ("\r\n" | "\n")? ~ EOI }
